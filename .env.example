# ACP endpoint for the local OpenCode agent used by Browser Use.
OPENCODE_ACP_URL=http://127.0.0.1:4096/acp
OPENCODE_MODEL=MiniMax-M2.7-highspeed
# Default output path for the scrape-glints command.
JOB_AUTOMATION_OUTPUT=output/glints-jobs.json
